name: Lint (disabled)

# This workflow is intentionally disabled so that Ruff/pre-commit run only in local environments.
# Retained as a placeholder to avoid accidental re-introduction via automation.
on:
  workflow_dispatch:

jobs:
  noop:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Ruff/pre-commit lint is disabled in CI."
